import { Avatar, Button } from "@ui-kitten/components";
import React from "react";

const VolProfile = () => {
	return (
		<>
			<Avatar source={require("../../images/")} />
			<Button>NAME PLACEHOLDER</Button>
		</>
	);
};

export default VolProfile;
